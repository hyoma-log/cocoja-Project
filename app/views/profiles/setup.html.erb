<div class="min-h-screen flex flex-col">
  <main class="flex-grow flex flex-col items-center px-4 py-8">
    <h2 class="text-center text-[20px] font-bold text-gray-900 mb-8">
      ご登録ありがとうございます<br>
      <span class="text-sm text-gray-600">
        ユーザー名とユーザーIDを登録して<br>
        ココじゃをはじめよう！
      </span>
    </h2>

    <% if @google_connected %>
      <div class="mb-6 bg-blue-50 border border-blue-200 rounded-md p-4">
        <p class="text-sm text-blue-700">
          Googleアカウントでログインしました。<br>続けてプロフィール情報を設定してください。
        </p>
      </div>
    <% end %>

    <%= form_with(model: @user, url: profile_update_path, html: { class: "w-full max-w-md space-y-6" }) do |f| %>
      <%= render "shared/error_messages", resource: @user if @user.errors.any? %>

      <div class="space-y-6">
        <div>
          <%= f.label :uid, "ユーザーID", class: "block text-sm font-semibold text-gray-800" %>
          <p class="text-xs text-gray-500 mb-1">半角英数字6〜15文字</p>
          <%= f.text_field :uid, class: "w-full px-4 py-2 text-gray-700 bg-white border rounded-lg focus:ring-2 focus:ring-blue-400 focus:outline-none" %>
        </div>

        <div>
          <%= f.label :username, "ユーザー名", class: "block text-sm font-semibold text-gray-800" %>
          <p class="text-xs text-gray-500 mb-1">1〜20文字</p>
          <%= f.text_field :username, class: "w-full px-4 py-2 text-gray-700 bg-white border rounded-lg focus:ring-2 focus:ring-blue-400 focus:outline-none" %>
        </div>
      </div>

      <div class="pt-8 flex justify-center">
        <%= f.submit "プロフィールを設定する", class: "w-[240px] bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full focus:ring-2 focus:ring-blue-400 focus:outline-none transition duration-200" %>
      </div>
    <% end %>
  </main>

  <small class="block text-center text-xs text-gray-400 m-4">© <%= Time.current.year %> cocoja</small>
</div>